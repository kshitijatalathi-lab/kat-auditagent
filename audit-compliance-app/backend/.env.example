# MCP Hybrid Backend
# Copy to .env and fill values as needed

# Toggle mock mode to avoid real external calls
LLM_MOCK=1

# MCP servers
MCP_GDRIVE_URL=http://localhost:8787
MCP_LLM_URL=http://localhost:8788

# Provider keys (only needed if integrating real LLMs later)
OPENAI_API_KEY=
GOOGLE_API_KEY=
GROQ_API_KEY=

# FastAPI
HOST=127.0.0.1
PORT=8010
